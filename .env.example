# Copy to ".env" and fill in:
TELEGRAM_BOT_TOKEN=123456:ABCDEF-your-token-here
# Comma-separated list of Telegram user IDs who are allowed to use this bot.
# For now put just your own ID (you can get it by messaging @userinfobot)
ALLOWED_USER_IDS=123456789

# Optional HTTP request defaults for endpoint checks
DEFAULT_TIMEOUT_SECONDS=3.0
DEFAULT_LATENCY_THRESHOLD_SECONDS=2.0
